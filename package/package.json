{
  "name": "atomic-devtools",
  "version": "1.0.90",
  "description": "Library to connect your Jotai application to Atomic Devtools Extension",
  "main": "dist/module/index.js",
  "files": [
    "dist"
  ],
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "compile": "babel --root-mode upward src/index.jsx  --out-dir dist/module",
    "clean": "rm -rf dist",
    "build": "npm run clean && npm run compile",
    "patch:publish": "npm version patch && npm publish"
  },
  "peerDependencies": {
    "jotai": "^0.15.3",
    "react": "^17.0.1",
    "react-dom": "^17.0.1"
  },
  "author": "",
  "license": "MIT"
}
